/**
 * 拖动走 Tauri 2 原生 OS 级 drag region。
 * 模板里在 title bar 元素上加 `data-tauri-drag-region` 即可，无需 JS。
 *
 * 这个 composable 留作占位，未来如果要支持"标题栏内某些区域不拖动"
 * （比如双击标题最大化）再扩展。
 */
export function useDraggable() {
  return {};
}
